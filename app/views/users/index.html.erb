<% provide(:title, 'All users') %>
<h1>All users</h1>
<%= will_paginate %>

<ul class="users">
   <%# Rails magic this will render _users.html.erb for each user in @users %>
  <%= render @users %>
</ul>
<%= will_paginate %>
